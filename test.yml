config:
  target: "https://role-based-ticket-management-tool.onrender.com"
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - flow:
      - post:
          url: "/api/v1/user/loginUser"
          json:
            email: "test@mail.com"
            password: "123456"